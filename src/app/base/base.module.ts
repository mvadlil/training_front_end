import { NgModule } from '@angular/core';

import { DefaultLanguageState } from './default-language/default-language.state';

@NgModule({
  declarations: [],
  imports: [],
  exports: [],
  providers: [
    DefaultLanguageState,
  ],
})
export class BaseModule { }
